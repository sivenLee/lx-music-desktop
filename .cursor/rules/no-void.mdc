---
description: ESLint no-void — 禁止使用 void 运算符
globs: "**/*.{js,ts,vue}"
alwaysApply: true
---

# no-void

本项目启用 ESLint `no-void`，**禁止**使用 `void` 运算符。

## 不要这样写

```js
void dialog('提示')
void handleAsync(index)
void somePromise
```

## 应这样写

```js
// 在 async 函数内：await
await dialog('提示')
await handleAsync(index)

// 非 async / 故意不阻塞：用 .catch（不要用 void）
dialog('提示').catch(() => {})
handleAsync(index).catch(err => {
  console.log(err)
})
```

本项目同时启用 `no-void` 与 `@typescript-eslint/no-floating-promises`：  
禁止 `void`，对 Promise 用 `await` 或 `.catch()` / 带 rejection handler 的 `.then()`。
